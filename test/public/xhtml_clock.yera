xmlns = null;

local = 0 until (lift \x x0 -> x - x0) (futr time) (pres time);

sec = second time;
min = minute time;
hor = hour time;

h0 = floor (hor / 10);
h1 = hor % 10;

m0 = floor (min / 10);
m1 = min % 10;

s0 = floor (sec / 10);
s1 = sec % 10;

text_time = "" + h0 + h1 + ":" + m0 + m1 + ":" + s0 + s1;

style = {
  background: "red"
  ,width: "80px"
  ,verticalAlign: "middle"
--  ,opacity: 0.3 
};
   
clock = `<center>
       <div style = style>$text_time</div>
       </center>;





